# device-platform-thing-model

最小物模型内核用于回答三个问题：设备有什么能力、原始协议数据如何映射为统一消息、消息是否符合物模型约束。

包职责：

| 包 | 责任 |
| --- | --- |
| `core` | 与厂商协议无关的能力、模型、消息和校验内核 |
| `adapter.djidock` | DJI Dock 风格 Topic/载荷到标准消息的转换示例 |
| `scenario` | 两组模拟数据与可直接运行的入口 |

这不是 MQTT、数据库或生产设备状态实现；这些能力会在后续独立模块中围绕标准消息接入。

