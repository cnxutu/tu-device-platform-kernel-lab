# tu-device-platform-kernel-lab

这是用于拆解复杂设备平台场景的独立可运行实验仓库。

- 父工程只负责模块编排与统一构建配置；领域实现放在对应的子模块中。
- 包路径以 `com.tu.deviceplatform.<module>` 开始，按 `core`、`adapter`、`scenario` 等职责分层。
- 新场景应先补充可运行示例和自动化测试，再抽象为可复用内核能力。
- 不复制生产仓库中的凭据、环境地址、真实设备编号或完整业务实现；示例数据必须是模拟数据。

